# Rust — артефакты Cargo во всех крейтах (в т.ч. siem-tools и новые)
target/

# Secrets — НИКОГДА не коммитим в репозиторий
# Файлы секретов в корне (устаревшие пути)
clickhouse_password.txt
smtp_password.txt
slack_webhook_url.txt
pagerduty_key.txt
minio_secret_key.txt
*_password.txt
*_secret_key.txt
*_webhook_url.txt
*_key.txt
*.secret

# Файлы секретов в директории деплоя
deploy/docker/secrets/*.txt
deploy/docker/secrets/*.secret
deploy/docker/certs/
!deploy/docker/secrets/README.md
!deploy/docker/secrets/.gitkeep

# GeoIP базы данных
*.mmdb

# Локальные конфиги с паролями
*.local.yaml
*.local.env
.env
deploy/docker/.env

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Frontend (case-management UI)
case-management/web/node_modules/
siem-portal/web/node_modules/

# SIEM Portal — артефакты Vite встраиваются в бинарь при сборке (см. siem-portal/web: npm run build; CI/Dockerfile делают это сами)
siem-portal/static/assets/
siem-portal/static/index.html

# Python caches
__pycache__/
*.pyc

